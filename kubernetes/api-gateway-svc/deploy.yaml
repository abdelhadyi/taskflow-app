apiVersion: apps/v1
kind: Deployment
metadata:
  name: taskflow-apigateway-deployment
  labels:
    app: taskflow-apigateway
spec:
  replicas: 3
  selector:
    matchLabels:
      app: taskflow-apigateway
  template:
    metadata:
      labels:
        app: taskflow-apigateway
    spec:
      containers:
      - name: apigateway
        image: abdelhadii/taskflow-apigateway:24808901511
        ports:
        - containerPort: 3000

